# infrastructure/docker

Container definitions for `apps/web`, `apps/api`, and local dev dependencies. Not created yet —
depends on the backend framework (ADR-004) and hosting target (ADR-015) being resolved first.
