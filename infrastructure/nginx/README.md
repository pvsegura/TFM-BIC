# infrastructure/nginx

Reverse-proxy / static-serving config (security headers, routing between `apps/web` and
`apps/api`). Not created yet — see
[security-baseline.md](../../docs/security/security-baseline.md) for the secure-headers
requirement this config must eventually satisfy.
