# content/languages

Per-language content trees, addressed by `languageId`. Currently only `pl/` (Polish) exists, as a
folder skeleton (no lesson content authored yet). See
[docs/architecture/content-architecture.md](../../docs/architecture/content-architecture.md) for
the full layout and rules (no per-language code, only per-language data).
